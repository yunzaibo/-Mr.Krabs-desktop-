/** 桌面端用户标识 */
export const DESKTOP_USER_ID = 'desktop-user'

/** 后端/前端约定的用户取消错误消息 */
export const USER_CANCELLED_MESSAGE = '用户取消'

/**
 * 会话标题缺失时的默认标题（非组件上下文 fallback）。
 * 组件中优先使用 i18n key: chat.newSessionDefault
 */
export const DEFAULT_SESSION_TITLE = 'New Chat'
